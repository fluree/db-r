# Fluree Memory — repo-scoped
# Auto-managed by `fluree memory`. Manual edits are supported.
@prefix mem: <https://ns.flur.ee/memory#> .
@prefix xsd: <http://www.w3.org/2001/XMLSchema#> .

mem:artifact-01kjdzhtx6em3sg77t7faxyzpz a mem:Artifact ;
    mem:content "The `config_iris` module in `fluree-vocab/src/lib.rs` (line 1673–1838) defines ~50 full-IRI string constants for the Fluree ledger config graph vocabulary. All IRIs live under `https://ns.flur.ee/db#`.\n\n**Organization by section:**\n- **Type classes** — `LEDGER_CONFIG`, `GRAPH_CONFIG`, `GRAPH_REF`, `TRUST_POLICY`\n- **Setting group predicates** — `POLICY_DEFAULTS`, `SHACL_DEFAULTS`, `REASONING_DEFAULTS`, `DATALOG_DEFAULTS`, `GRAPH_OVERRIDES`\n- **Policy fields** — `DEFAULT_ALLOW`, `POLICY_SOURCE`\n- **SHACL fields** — `SHACL_ENABLED`, `SHAPES_SOURCE`, `VALIDATION_MODE`, `VALIDATION_REJECT`, `VALIDATION_WARN`\n- **Reasoning fields** — `REASONING_MODES`, `SCHEMA_SOURCE`\n- **Datalog fields** — `DATALOG_ENABLED`, `RULES_SOURCE`, `ALLOW_QUERY_TIME_RULES`\n- **Override control** — `OVERRIDE_CONTROL`, `CONTROL_MODE`, `ALLOWED_IDENTITIES`, `OVERRIDE_NONE`, `OVERRIDE_ALL`, `IDENTITY_RESTRICTED`\n- **GraphConfig fields** — `TARGET_GRAPH`, `DEFAULT_GRAPH` (sentinel for g_id=0), `TXN_META_GRAPH` (sentinel for g_id=1)\n- **GraphRef / GraphSource fields** — `GRAPH_SOURCE`, `LEDGER_PRED`, `GRAPH_SELECTOR`\n- **TrustPolicy fields** — `TRUST_POLICY_PRED`, `TRUST_MODE`, `TRUSTED`, `SIGNED_INDEX`, `COMMIT_VERIFIED`\n- **Temporal** — `AT_T`\n- **Rollback guard** — `ROLLBACK_GUARD`, `MIN_T`\n- **Transact defaults** — `TRANSACT_DEFAULTS`, `UNIQUE_ENABLED`, `CONSTRAINTS_SOURCE`, `ENFORCE_UNIQUE`\n\n**Primary consumer:** `fluree-db-api/src/config_resolver.rs` — reads the config graph by encoding these IRIs into SIDs and querying the ledger snapshot. The config resolver uses these constants to hydrate `LedgerConfig`, `GraphConfig`, `PolicyDefaults`, `ShaclDefaults`, `ReasoningDefaults`, `DatalogDefaults`, trust policies, rollback guards, and override controls.\n\n**Sentinel IRIs:** `DEFAULT_GRAPH` and `TXN_META_GRAPH` are sentinel values used in `matches_graph_target()` to match graph overrides — `DEFAULT_GRAPH` matches `None` (g_id=0) and `TXN_META_GRAPH` matches any IRI ending with `#txn-meta`." ;
    mem:tag "IRI-constants" ;
    mem:tag "config-graph" ;
    mem:tag "config-iris" ;
    mem:tag "config-resolver" ;
    mem:tag "ledger-config" ;
    mem:tag "vocab" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:artifactRef "fluree-db-api/src/config_resolver.rs" ;
    mem:artifactRef "fluree-vocab/src/lib.rs" ;
    mem:branch "feature/config-graph" ;
    mem:createdAt "2026-02-26T22:01:47.686885+00:00"^^xsd:dateTime ;
    mem:artifactKind "module" .

mem:artifact-01kjte32vm4nfdfmxwvka0nqze a mem:Artifact ;
    mem:content "SPARQL Compliance Key Files Quick Reference\n\nTest infrastructure:\n- testsuite-sparql/tests/w3c_sparql.rs — Test entry points (all test functions)\n- testsuite-sparql/src/lib.rs — check_testsuite() harness entry point\n- testsuite-sparql/src/manifest.rs — W3C manifest.ttl parsing (TestManifest iterator)\n- testsuite-sparql/src/query_handler.rs — Eval test handler: data load, query exec, result compare\n- testsuite-sparql/src/result_comparison.rs — Isomorphic result comparison (blank node mapping)\n- testsuite-sparql/src/result_format.rs — Parse .srx/.srj/.ttl/.rdf expected results\n- testsuite-sparql/src/report.rs — JSON report generation\n- testsuite-sparql/src/subprocess.rs — Subprocess isolation with timeout enforcement\n- testsuite-sparql/src/bin/run_w3c_test.rs — Subprocess worker binary\n- testsuite-sparql/Makefile — Developer convenience targets\n\nSPARQL pipeline:\n- fluree-db-sparql/src/parser/ — SPARQL parser (grammar rules)\n- fluree-db-sparql/src/lower/ — SPARQL→IR lowering (expression.rs, aggregate.rs, pattern.rs, path.rs, ask.rs, construct.rs, select.rs)\n\nJSON-LD pipeline:\n- fluree-db-query/src/parse/lower.rs — JSON-LD→IR lowering\n\nShared execution:\n- fluree-db-query/src/ir.rs — Shared IR types (Query, Pattern, Expression, etc.)\n- fluree-db-query/src/expression/ — Expression evaluation (string.rs, numeric.rs, etc.)\n- fluree-db-query/src/aggregate.rs — Aggregate function execution\n- fluree-db-query/src/execute.rs — Query execution engine\n\nIntegration tests:\n- fluree-db-api/tests/it_query_sparql.rs — SPARQL integration tests\n- fluree-db-api/tests/it_query.rs — JSON-LD query integration tests\n- fluree-db-api/tests/it_query_analytical.rs — Analytical/grouping JSON-LD tests\n- fluree-db-api/tests/it_query_grouping.rs — GROUP BY JSON-LD tests\n\nDocumentation:\n- docs/contributing/sparql-compliance.md — Full compliance testing guide\n- Cargo.toml line 41-42 — workspace exclude for testsuite-sparql" ;
    mem:tag "compliance" ;
    mem:tag "files" ;
    mem:tag "key-files" ;
    mem:tag "reference" ;
    mem:tag "sparql" ;
    mem:tag "testsuite-sparql" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:artifactRef "docs/contributing/sparql-compliance.md" ;
    mem:artifactRef "fluree-db-query/src/expression/" ;
    mem:artifactRef "fluree-db-query/src/ir.rs" ;
    mem:artifactRef "fluree-db-sparql/src/lower/" ;
    mem:artifactRef "testsuite-sparql/" ;
    mem:branch "main" ;
    mem:createdAt "2026-03-03T18:06:46.132479+00:00"^^xsd:dateTime ;
    mem:artifactKind "file" .

mem:constraint-01kj8k4mfr2w5h0k2w67w07nz8 a mem:Constraint ;
    mem:content "Important patterns and gotchas:\n- Binary index PSOT range queries may return a SUPERSET of matching flakes — post-filtering by subject/predicate is required (see datalog_rules.rs fix)\n- `spawn_blocking` + `handle.block_on()` pattern in indexer for non-Send types across await points\n- `max_t` tracking in index_build.rs must happen BEFORE retraction skip (op=0) — otherwise retraction-only commits don't update max_t, causing waiter deadlocks" ;
    mem:tag "deadlock" ;
    mem:tag "gotchas" ;
    mem:tag "max-t" ;
    mem:tag "patterns" ;
    mem:tag "post-filtering" ;
    mem:tag "psot" ;
    mem:tag "range-queries" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:severity "must" ;
    mem:artifactRef "fluree-db-indexer/src/index_build.rs" ;
    mem:branch "feature/memory" ;
    mem:createdAt "2026-02-24T19:48:40.056044+00:00"^^xsd:dateTime .

mem:constraint-01kjcyjgszs18b9h4jg7h3xpfj a mem:Constraint ;
    mem:content "Consensus/multisig gotcha for config graph: commit validity rules (validator set/threshold) used to validate a proposed commit at t must come from already-committed state (effectively config as-of t-1), not from config mutations inside the same commit; otherwise a commit could authorize itself by changing validator set/threshold in its own payload. Config can declare consensus parameters, but validation must treat them as lagging state." ;
    mem:tag "commit-validation" ;
    mem:tag "config-graph" ;
    mem:tag "consensus" ;
    mem:tag "gotcha" ;
    mem:tag "multisig" ;
    mem:tag "raft" ;
    mem:tag "tendermint" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:createdAt "2026-02-26T12:25:27.104151+00:00"^^xsd:dateTime .

mem:constraint-01kjd0p1bpj6n5nw00ssap82h0 a mem:Constraint ;
    mem:content "Memory hygiene when recalling: if memory_recall returns multiple overlapping/near-duplicate memories about the same concept, consolidate them into a single canonical memory. Prefer creating the canonical version via memory_update so it supersedes an older entry (superseded entries are filtered from \"current\" results). For any remaining duplicates, either forget them if they add no unique value, or rewrite them into a short pointer that links to the canonical memory ID." ;
    mem:tag "agent-rules" ;
    mem:tag "dedup" ;
    mem:tag "hygiene" ;
    mem:tag "memory" ;
    mem:tag "recall" ;
    mem:tag "supersedes" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:severity "should" ;
    mem:artifactRef ".cursor/rules/fluree_rules.md" ;
    mem:createdAt "2026-02-26T13:02:19.510395+00:00"^^xsd:dateTime .

mem:constraint-01kjte29y3cp8mae56xy6yfv2n a mem:Constraint ;
    mem:content "SPARQL↔JSON-LD Parity: Must-Check When Adding SPARQL Features\n\nWhen implementing a new SPARQL feature from W3C compliance work (#55 sub-issues):\n\n1. **Shared IR change?** If adding a new Expression/Pattern/AggregateFn variant or modifying execution operators, it ALREADY affects JSON-LD query — ensure JSON-LD tests still pass.\n\n2. **Expressible in JSON-LD?** If the feature can be expressed in JSON-LD query syntax (new function, filter operator, aggregation mode), add JSON-LD query tests too — don't leave a parity gap.\n\n3. **Execution path missing entirely?** (e.g., VALUES, certain property path modes) — SPARQL lowering adds the IR pattern but JSON-LD lowering (`fluree-db-query/src/parse/lower.rs`) may not produce it yet. Track as a follow-up issue.\n\n4. **Test locations:**\n   - SPARQL integration: `fluree-db-api/tests/it_query_sparql.rs`\n   - JSON-LD integration: `fluree-db-api/tests/it_query.rs`, `it_query_analytical.rs`, `it_query_grouping.rs`\n   - Shared expression/execution unit tests: `fluree-db-query/src/` modules\n\n5. **Validation after shared-code changes:**\n   ```\n   cd testsuite-sparql &amp;&amp; make test-eval-cat CAT=&lt;category&gt;   # SPARQL W3C\n   cd .. &amp;&amp; cargo test -p fluree-db-api --test it_query        # JSON-LD\n   ```\n\nRationale: Both query languages compile to the same IR (fluree-db-query/src/ir.rs) and share the entire execution engine. A change to shared code that fixes SPARQL but breaks JSON-LD query is unacceptable." ;
    mem:tag "compliance" ;
    mem:tag "constraint" ;
    mem:tag "jsonld" ;
    mem:tag "must-check" ;
    mem:tag "parity" ;
    mem:tag "sparql" ;
    mem:tag "testing" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:severity "must" ;
    mem:artifactRef "fluree-db-api/tests/it_query.rs" ;
    mem:artifactRef "fluree-db-api/tests/it_query_sparql.rs" ;
    mem:artifactRef "fluree-db-query/src/ir.rs" ;
    mem:branch "main" ;
    mem:createdAt "2026-03-03T18:06:20.611062+00:00"^^xsd:dateTime .

mem:decision-01kjc0f2eze1q1sq7ke1b3z13c a mem:Decision ;
    mem:content "memory_recall smart score filtering heuristic (MCP tool only):\n\nWhen the LLM calls memory_recall with NO explicit limit or offset, apply score-based trimming before returning results:\n- Fetch limit+1 candidates (the extra is a peek-ahead for reporting next score)\n- Threshold: top_score * 0.5 — drop any result below 50% of the top score\n- Always return at least 1 result (even if everything is below threshold)\n- Report the score of the first non-returned result in the <pagination> element so the LLM can decide whether to fetch more\n- Pagination hint: \"Results 1–N, next score: X.X. Use offset=N to retrieve more.\"\n\nWhen explicit limit or offset IS provided, bypass all smart filtering — honor the request exactly.\n\nKey files: fluree-db-memory/src/mcp.rs (use_smart_filter logic), fluree-db-memory/src/format.rs (next_score: Option<f64> param added to format_context_paged)" ;
    mem:tag "bm25" ;
    mem:tag "heuristic" ;
    mem:tag "mcp" ;
    mem:tag "memory" ;
    mem:tag "recall" ;
    mem:tag "scoring" ;
    mem:tag "ux" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:branch "feature/memory" ;
    mem:createdAt "2026-02-26T03:39:16.831509+00:00"^^xsd:dateTime ;
    mem:rationale "Without filtering, 3 results were always returned even when the 2nd and 3rd had scores far below the top hit (e.g., 69.4 / 26.8 / 7.0). Low-confidence memories waste context and can mislead the LLM. The 50% threshold is a natural gap detector: when a strong match exists, it dominates and only truly related memories pass the threshold." ;
    mem:alternatives "Fixed limit reduction (e.g., always return 1); percentile-based cutoff; absolute score floor. The relative 50% threshold was chosen because BM25 scores are query-relative and unnormalized — a relative threshold adapts naturally to query strength." .

mem:decision-01kje2pt9maxrxjw49ydgejmpe a mem:Decision ;
    mem:content "f:enforceUnique uses storage-layer value identity, NOT RDF strict equality. The uniqueness key is (graph, predicate, FlakeValue) — it ignores the RDF datatype IRI (flake.dt) and language tag (FlakeMeta.lang). This matches \"how humans think of the value\": if the bytes are the same, it's a duplicate regardless of what datatype or language annotation is attached. Callers cannot subvert uniqueness by asserting the same string with a different datatype IRI or lang tag. Different FlakeValue variants (e.g., FlakeValue::String(\"42\") vs FlakeValue::Long(42)) are naturally distinct at the storage layer and do NOT conflict. The RangeMatch query uses predicate_object(p, o) without .with_datatype(), which matches all datatypes for that (p, o) pair in the POST index." ;
    mem:tag "FlakeValue" ;
    mem:tag "POST-index" ;
    mem:tag "RDF" ;
    mem:tag "config-graph" ;
    mem:tag "datatype" ;
    mem:tag "enforceUnique" ;
    mem:tag "transact" ;
    mem:tag "uniqueness" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:artifactRef "fluree-db-api/src/tx.rs" ;
    mem:branch "feature/config-graph" ;
    mem:createdAt "2026-02-26T22:56:56.628412+00:00"^^xsd:dateTime ;
    mem:rationale "RDF strict equality treats \"hello\"^^xsd:string and \"hello\"^^ex:custom as different values because their datatype IRIs differ. For a uniqueness constraint this is surprising and easily subverted — users expect \"same text = same value.\" Storage-layer identity (FlakeValue enum variant + payload) is the right granularity: it's what the index actually stores and compares, it's deterministic, and it prevents accidental or intentional duplicate sneaking via datatype annotation." ;
    mem:alternatives "RDF strict equality (datatype-aware key including dt Sid), datatype-family matching via dt_compatible()" .

mem:decision-01kjtd4ddbk45wadcafd8tfea7 a mem:Decision ;
    mem:content "When a novelty overlay is present, the fast POST leaflet-skipping operators (`PredicateGroupCountFirstsOperator`, `PredicateObjectCountFirstsOperator`) can stay on the fast base path and apply an exact overlay delta by translating overlay flakes to `OverlayOp` IDs and simulating per-fact state transitions in time order. We infer initial presence from the first op (first retract => existed pre-overlay) and adjust counts on assert/retract transitions; this avoids falling back to `range_with_overlay` for overlays." ;
    mem:tag "count" ;
    mem:tag "leaflet" ;
    mem:tag "novelty" ;
    mem:tag "overlay" ;
    mem:tag "performance" ;
    mem:tag "post" ;
    mem:tag "query" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:artifactRef "fluree-db-query/src/binary_scan.rs" ;
    mem:artifactRef "fluree-db-query/src/fast_group_count_firsts.rs" ;
    mem:createdAt "2026-03-03T17:50:01.131764+00:00"^^xsd:dateTime ;
    mem:rationale "Preserve the leaflet FIRST-skipping performance while still producing correct results under novelty, given the invariant that overlay retractions only target facts that existed at the time." ;
    mem:alternatives "Always fall back when overlay/policy/history is active; use BinaryCursor merge for overlay even in fast-path; do per-assert base existence checks." .

mem:fact-01kj8k4e2c8qztn5d52w6z1p3z a mem:Fact ;
    mem:content "Key architecture structs and renames:\n- `LedgerSnapshot` in fluree-db-core/src/db.rs (renamed from `Db`) — immutable index snapshot at a point in time\n- `GraphDb` in fluree-db-api/src/view/types.rs (renamed from `FlureeView`) — queryable database value with overlay, policy, reasoning\n- `BinaryGraphView` in fluree-db-binary-index (moved from indexer) — graph-scoped binary index handle\n- `DataSetDb` (renamed from `FlureeDataSetView`) — multi-graph dataset\n- `CachedLedgerState` (renamed from `LedgerSnapshot` in ledger_manager.rs) — cached ledger state\n- `RangeProvider` trait — delegated range queries (binary index), g_id passed per-call\n- `BinaryRangeProvider` wraps `BinaryIndexStore` for PSOT/SPOT/etc queries (no embedded g_id)\n- `range_with_overlay()` in fluree-db-core/src/range.rs — main range query function (takes g_id param)\n- `BackgroundIndexerWorker` with `IndexCompletion` oneshot waiters for async indexing\n- Factory methods: `fluree.db()`, `fluree.db_at_t()`, `fluree.db_at()`, `fluree.db_with_policy()`\n- Dead code deleted: fluree-db-query/src/graph_view.rs (entire module), `*_for_view()` methods in enforcer" ;
    mem:tag "architecture" ;
    mem:tag "graph-db" ;
    mem:tag "ledger-snapshot" ;
    mem:tag "range-provider" ;
    mem:tag "renames" ;
    mem:tag "structs" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:artifactRef "fluree-db-api/src/view/types.rs" ;
    mem:artifactRef "fluree-db-core/src/db.rs" ;
    mem:artifactRef "fluree-db-core/src/range.rs" ;
    mem:branch "feature/memory" ;
    mem:createdAt "2026-02-24T19:48:33.484178+00:00"^^xsd:dateTime ;
    mem:factKind "architecture" .

mem:fact-01kj8k4gvpbtb4njvzqt6tdz1g a mem:Fact ;
    mem:content "Index pipeline:\n- `build_binary_index()` in fluree-db-indexer/src/lib.rs — direct entry point (takes NsRecord, always builds)\n- `build_index_for_ledger()` — wraps above with nameservice lookup + early-return if current\n- `reindex()` in fluree-db-api/src/admin.rs uses `build_binary_index` for forced rebuilds\n- Old B-tree pipeline fully removed (BatchedRebuild, checkpoints, ReindexProgress, etc.)" ;
    mem:tag "binary-index" ;
    mem:tag "index-pipeline" ;
    mem:tag "indexer" ;
    mem:tag "reindex" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:artifactRef "fluree-db-api/src/admin.rs" ;
    mem:artifactRef "fluree-db-indexer/src/lib.rs" ;
    mem:branch "feature/memory" ;
    mem:createdAt "2026-02-24T19:48:36.342399+00:00"^^xsd:dateTime ;
    mem:factKind "architecture" .

mem:fact-01kj8k4q4rysa5ba61nxjngzmw a mem:Fact ;
    mem:content "Commit format (commit-v2):\n- `CommitV2Header` fields: version, flags, t, op_count, envelope_len, sig_block_len\n- `CommitV2Envelope` fields include txn_signature (Option&lt;TxnSignature&gt;)\n- `Commit` struct has `txn_signature: None, commit_signatures: Vec::new()` for test construction\n- Magic bytes: `FCV2` (binary commit-v2 only; legacy JSON path fully removed)" ;
    mem:tag "FCV2" ;
    mem:tag "binary-format" ;
    mem:tag "commit-format" ;
    mem:tag "commit-v2" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:branch "feature/memory" ;
    mem:createdAt "2026-02-24T19:48:42.776648+00:00"^^xsd:dateTime ;
    mem:factKind "architecture" .

mem:fact-01kj8k4v897jghnsy7dtjpg475 a mem:Fact ;
    mem:content "OPST index changes:\n- OPST now stores ALL object types (not just refs) — changed in run_writer.rs, leaflet.rs, binary_index_store.rs\n- Leaflet format: o_kind[u8], RLE(o_key:u64), p_id[pw], s_id[u64] (breaking change, requires rebuild)\n- `is_iri_ref()` on RunRecord removed (was only used for OPST filtering)\n- OPST guard in `translate_range()` removed" ;
    mem:tag "breaking-change" ;
    mem:tag "index-format" ;
    mem:tag "leaflet" ;
    mem:tag "object-types" ;
    mem:tag "opst" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:branch "feature/memory" ;
    mem:createdAt "2026-02-24T19:48:46.985249+00:00"^^xsd:dateTime ;
    mem:factKind "architecture" .

mem:fact-01kj8k4wy34fgvmg3r6vt142pm a mem:Fact ;
    mem:content "Stats pipeline (bulk import):\n- `IdStatsHook` collects per-property HLL stats during commit resolution (stats.rs)\n- `on_record()` signature: (g_id, p_id, s_id, dt, o_hash, o_kind, o_key, t, op) — includes object for class tracking\n- `set_rdf_type_p_id()` enables class tracking; rdf:type pre-inserted into predicate dict before resolution\n- `finalize_with_aggregate_properties()` returns (IdStatsResult, Vec&lt;GraphPropertyStatEntry&gt;, class_counts, class_properties)\n- Class property attribution: tracks subject_classes + subject_props maps; handles both orderings (type before/after props)\n- Stats JSON includes top-level `properties` (SID-keyed), `classes` (with property usages), and per-graph `graphs`\n- Class JSON format: [[ns_code, \"suffix\"], [count, [[[prop_ns, prop_name]], ...]]]\n- `StatsView::from_db_stats_with_namespaces()` derives IRI-keyed from SID-keyed using namespace_codes" ;
    mem:tag "bulk-import" ;
    mem:tag "class-tracking" ;
    mem:tag "hll" ;
    mem:tag "stats" ;
    mem:tag "stats-pipeline" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:branch "feature/memory" ;
    mem:createdAt "2026-02-24T19:48:48.707971+00:00"^^xsd:dateTime ;
    mem:factKind "architecture" .

mem:fact-01kj8k505wm9rz9609edzry5r8 a mem:Fact ;
    mem:content "BinaryRangeProvider / LedgerState loading (fixed):\n- `fluree.ledger()` in loading.rs now attaches BinaryRangeProvider + binary_store from index root\n- `TypeErasedStore(Arc&lt;dyn Any + Send + Sync&gt;)` for cross-crate type erasure (fluree-db-ledger → fluree-db-indexer)\n- `execute_query_internal` uses `prepare_execution` + `execute_prepared` with `ContextConfig` containing binary_store\n- `upload_dicts_to_cas()` must strip namespace prefixes before building subject trees (matches `upload_dicts_from_disk`)\n- `resolve_subject_iri` has defensive check against double-prefixing (CAS trees may store full IRIs)\n- `cache_bytes_to_file` uses atomic counter for unique tmp names (race condition fix)" ;
    mem:tag "binary-range-provider" ;
    mem:tag "ledger-state" ;
    mem:tag "loading" ;
    mem:tag "query-execution" ;
    mem:tag "type-erasure" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:artifactRef "fluree-db-api/src/loading.rs" ;
    mem:branch "feature/memory" ;
    mem:createdAt "2026-02-24T19:48:52.028106+00:00"^^xsd:dateTime ;
    mem:factKind "architecture" .

mem:fact-01kj8k54dgr5qsah3cfbbdp4xs a mem:Fact ;
    mem:content "Namespace augmentation after indexing (fixed):\n- BinaryIndexStore.namespace_codes only has namespaces from index root\n- Novelty commits can introduce new namespace codes not in the index\n- `DictOverlay.resolve_subject_iri()` delegates to `store.namespace_prefix()` → fails for new codes\n- Fix: `BinaryIndexStore::augment_namespace_codes(&amp;mut self, extra)` merges novelty ns codes + rebuilds prefix_trie\n- Called in 4 store-loading paths (before Arc::new): loading.rs (Fluree::ledger), ledger_manager.rs (load_and_attach_binary_store), fluree_ext.rs (load_view, load_view_at_t)\n- Note: cached path in `apply_index_v2` creates store from root; subsequent novelty commits adding new ns codes would need store invalidation or DictOverlay fallback" ;
    mem:tag "augmentation" ;
    mem:tag "dict-overlay" ;
    mem:tag "namespace" ;
    mem:tag "namespace-codes" ;
    mem:tag "prefix-trie" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:artifactRef "fluree-db-api/src/fluree_ext.rs" ;
    mem:artifactRef "fluree-db-api/src/ledger_manager.rs" ;
    mem:artifactRef "fluree-db-api/src/loading.rs" ;
    mem:branch "feature/memory" ;
    mem:createdAt "2026-02-24T19:48:56.368004+00:00"^^xsd:dateTime ;
    mem:factKind "architecture" .

mem:fact-01kj8k5anm83eax0rx47tc92jd a mem:Fact ;
    mem:content "Spatial index pipeline (S2 complex geometries):\n- `fluree-db-spatial` crate: S2 cell-based indexing for polygons, linestrings, etc.\n- `SpatialHook` in indexer collects geo:wktLiteral during commit resolution (skips POINTs). SpatialEntry includes g_id for graph scoping.\n- `SpatialIndexBuilder.add_geometry()` → S2 covering → CellEntry records\n- `BuildResult.write_to_cas()` returns WriteResult with all CAS addresses for GC\n- `SpatialIndexRef` in IndexRoot.spatial_indexes tracks: g_id, predicate, root_address, manifest, arena, leaflets\n- Graph-scoped routing: indexes grouped by (g_id, p_id), provider map key format \"g{g_id}:{predicate_iri}\", S2SearchOperator uses ctx.binary_g_id, fallback to g_id=0\n- Loading: Fluree::ledger() calls load_spatial_indexes() → SpatialIndexSnapshot::load_from_cas()\n- Storage: LedgerState.spatial_indexes = TypeErasedStore(Arc&lt;HashMap&lt;String, Arc&lt;dyn SpatialIndexProvider&gt;&gt;&gt;)\n- Query wiring: execute_query_internal → ContextConfig.spatial_providers; FlureeView.spatial_indexes from LedgerState; query_historical() rejects S2 queries\n- Predicate-based routing: S2SearchPattern.predicate specifies which index (idx:property expanded via details())\n- Query syntax: { \"idx:spatial\": \"within\", \"idx:property\": \"ex:hasGeometry\", \"idx:geometry\": \"POLYGON(...)\", \"idx:result\": \"?place\" }" ;
    mem:tag "geometry" ;
    mem:tag "graph-scoped" ;
    mem:tag "polygon" ;
    mem:tag "s2" ;
    mem:tag "spatial" ;
    mem:tag "spatial-index" ;
    mem:tag "wkt" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:artifactRef "fluree-db-spatial/" ;
    mem:branch "feature/memory" ;
    mem:createdAt "2026-02-24T19:49:02.772655+00:00"^^xsd:dateTime ;
    mem:factKind "architecture" .

mem:fact-01kj8k5hrrafnwdwnjvvk8jk35 a mem:Fact ;
    mem:content "Storage-agnostic commits (CID migration) — core complete:\n- All identity fields are CID-based. Address strings are now derived internal details.\n- NsRecord: CID-only (commit_head_id, index_head_id). No commit_address/index_address.\n- LedgerState: CID-only (head_commit_id, head_index_id). No head_commit: Option&lt;String&gt;.\n- BinaryIndexRoot v3: All artifact refs are ContentId (DictRefs, GraphOrderRefs, etc.)\n- GraphSourceRecord: index_id: Option&lt;ContentId&gt; (no address field)\n- Commit/CommitRef/IndexRef: Use ContentId (no address/v/data fields)\n- CommitReceipt: Has commit_id: ContentId; address: Option&lt;String&gt; from storage write result\n- IndexResult: Has root_id: ContentId (root_address still derived for GC/Db::load)\n- Push/import integrity: commit_hash_hex_from_bytes() recomputes SHA-256, compares to trailer, rejects tampering. Chain continuity via previous_ref.id.\n- ContentId::verify(): Correct for txn/index blobs. NOT for commit-v2 (excludes trailing hash+sig). Commit uses commit_hash_hex_from_bytes().\n- Remaining derived-address affordances: IndexResult.root_address, CommitReceipt.address, Db::load(), GC pipeline" ;
    mem:tag "cid" ;
    mem:tag "commit" ;
    mem:tag "content-id" ;
    mem:tag "identity" ;
    mem:tag "integrity" ;
    mem:tag "sha256" ;
    mem:tag "storage-agnostic" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:branch "feature/memory" ;
    mem:createdAt "2026-02-24T19:49:10.040895+00:00"^^xsd:dateTime ;
    mem:factKind "architecture" .

mem:fact-01kj8k5nzpe59p5vp7zrehbrr9 a mem:Fact ;
    mem:content "Forward dictionary packing (feature/tsv-format branch):\n- Replaced CoW B-tree forward dicts (many ~2MB DLF1 CAS leaves) with large FPK1 pack files (~256MB)\n- FPK1 format: 40-byte header + concatenated pages + page directory. O(1) lookup via offset indexing\n- ForwardPackReader: mmap-backed multi-pack reader, binary search packs by ID range\n- DictionarySet now has subject_forward_packs: BTreeMap&lt;u16, ForwardPackReader&gt; + string_forward_packs: ForwardPackReader\n- write_subject_iri_bytes() / write_string_value_bytes() — zero-copy hot path for TSV export\n- IRB1 root bumped to v2; DictPackRefs replaces forward tree refs; reverse dicts unchanged (DLF1 tree)\n- db.rs lightweight IRB1 parser updated for v2 (version-conditional dict ref parsing)\n- Clean break — no backward compatibility with v1 forward trees\n- Key files: dict_tree/forward_pack.rs, dict_tree/pack_reader.rs, dict_tree/pack_builder.rs" ;
    mem:tag "dictionary" ;
    mem:tag "forward-dict" ;
    mem:tag "fpk1" ;
    mem:tag "mmap" ;
    mem:tag "pack-files" ;
    mem:tag "tsv-export" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:branch "feature/memory" ;
    mem:createdAt "2026-02-24T19:49:14.358637+00:00"^^xsd:dateTime ;
    mem:factKind "architecture" .

mem:fact-01kj8k5tywx7y294awm0g0cmas a mem:Fact ;
    mem:content "fluree-db-binary-index crate (extracted from indexer):\n- Owns on-disk binary index formats (IRB1, FBR2, FLI2, FPK1, DTB1, DLR1, VAS1, NB1) and read-side runtime\n- fluree-db-query depends on binary-index (not indexer) for read-side types\n- fluree-db-indexer depends on binary-index + provides backward-compat shim re-exports in run_index/mod.rs and dict_tree/mod.rs\n- Modules: format/ (run_record, index_root, branch, leaf, leaflet, stats_wire), dict/ (branch, builder, reader, forward_pack, pack_reader, etc.), read/ (binary_index_store, binary_cursor, spot_cursor, query, leaflet_cache, batched_lookup, replay), arena/ (numbig, vector), types.rs\n- dict/dict_io.rs and dict/global_dict.rs are trimmed read-only copies (write-side stays in indexer)\n- Key re-exports: BinaryIndexStore, BinaryGraphView, BinaryCursor, RunRecord, RunSortOrder, IndexRootV5, etc." ;
    mem:tag "binary-index" ;
    mem:tag "crate-extraction" ;
    mem:tag "fbr2" ;
    mem:tag "index-formats" ;
    mem:tag "irb1" ;
    mem:tag "read-side" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:artifactRef "fluree-db-binary-index/" ;
    mem:branch "feature/memory" ;
    mem:createdAt "2026-02-24T19:49:19.452768+00:00"^^xsd:dateTime ;
    mem:factKind "architecture" .

mem:fact-01kj8sz7j1ps9p8772n0kxga15 a mem:Fact ;
    mem:content "Building the CLI binary:\n- Crate: `fluree-db-cli`, binary name: `fluree`, output: `target/release/fluree`\n- Basic build: `cargo build -p fluree-db-cli --release`\n- With OTEL: `cargo build -p fluree-db-cli --features otel --release`\n- All features: `cargo build -p fluree-db-cli --all-features --release`\n- Features: `server` (default, embeds HTTP server subcommands), `otel` (OpenTelemetry tracing)\n- Without `server` feature, CLI operates in standalone mode (import/admin commands only)" ;
    mem:tag "binary" ;
    mem:tag "build" ;
    mem:tag "cli" ;
    mem:tag "otel" ;
    mem:tag "release" ;
    mem:tag "server" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:artifactRef "fluree-db-cli/Cargo.toml" ;
    mem:artifactRef "fluree-db-cli/src/main.rs" ;
    mem:branch "feature/memory" ;
    mem:createdAt "2026-02-24T21:48:03.009493+00:00"^^xsd:dateTime ;
    mem:factKind "command" .

mem:fact-01kje110tp07nwqae7av5amd5s a mem:Fact ;
    mem:content "f:enforceUnique implementation: transaction-time property uniqueness enforcement. Annotations (f:enforceUnique true) are triples on property IRIs in source graphs. Config activation via f:transactDefaults with f:uniqueEnabled and f:constraintsSource. Uses POST index with datatype-aware (g, p, o, dt) matching. Stale removal in range_with_overlay handles last-op-wins. Enforcement runs after staging, reads config from pre-txn state (lagging). Independent of shacl feature flag. Files: fluree-vocab config_iris (4 constants), ledger_config.rs TransactDefaults, config_resolver.rs (MergeableGroup + reader + merge_transact_opts), tx.rs (enforce_unique_after_staging → resolve_per_graph_unique_sids → enforce_unique_constraints)." ;
    mem:tag "config-graph" ;
    mem:tag "enforce-unique" ;
    mem:tag "transact-defaults" ;
    mem:tag "tx-pipeline" ;
    mem:tag "uniqueness" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:artifactRef "fluree-db-api/src/config_resolver.rs" ;
    mem:artifactRef "fluree-db-api/src/tx.rs" ;
    mem:artifactRef "fluree-db-core/src/ledger_config.rs" ;
    mem:artifactRef "fluree-db-transact/src/error.rs" ;
    mem:artifactRef "fluree-vocab/src/lib.rs" ;
    mem:branch "feature/config-graph" ;
    mem:createdAt "2026-02-26T22:27:33.846975+00:00"^^xsd:dateTime ;
    mem:factKind "architecture" .

mem:fact-01kje30pfdnvpv5fx0ng4234jf a mem:Fact ;
    mem:content "The `docs/` directory is the canonical documentation source of truth, containing 15 sections and ~124 markdown files. It is designed for future static-site generators (mdBook, Zola, Docusaurus) and renders natively on GitHub.\n\n**Navigation & discovery:**\n- `docs/SUMMARY.md` is the **table of contents** — a hierarchical list of every page. When adding a new doc, always add its entry here.\n- Each subdirectory has a `README.md` that acts as a **hub page** listing all docs in that section with brief descriptions. When adding a doc to a section, update that section's README too.\n- The top-level `docs/README.md` is a short meta-doc explaining authoring conventions (one topic per file, capability-based names, relative links).\n\n**Section layout:**\n| Section | Purpose |\n|---|---|\n| `cli/` | One file per CLI command (init, create, query, etc.) |\n| `getting-started/` | Quickstart guides (server, ledger, write, query, Rust API) |\n| `concepts/` | Core concepts (ledgers, IRIs, datatypes, datasets, time-travel, policy) |\n| `design/` | Deep-dive architecture docs (query execution, index format, storage traits, nameservice, commits) |\n| `api/` | HTTP API (endpoints, headers, signed requests, errors) |\n| `query/` | Query guides (JSON-LD, SPARQL, output formats, datasets, CONSTRUCT, explain) |\n| `transactions/` | Transaction guides (insert, upsert, update/delete, retractions, turtle, signing) |\n| `security/` | Auth, encryption, commit signing, policy model |\n| `indexing-and-search/` | Indexing, reindex, fulltext, BM25, vector, geospatial |\n| `graph-sources/` | External graph sources (Iceberg, R2RML, BM25) |\n| `operations/` | Config, storage backends, DynamoDB, telemetry, admin/health |\n| `troubleshooting/` | Common errors, debugging queries, performance tracing |\n| `reference/` | Glossary, vocabulary, crate-map, connection config, compatibility |\n| `contributing/` | Dev setup, tests, SPARQL compliance, tracing guide, design notes |\n\n**Where to put new content:**\n- **New CLI command** → `docs/cli/<command>.md` + entry in SUMMARY.md\n- **New concept or behavior explanation** → `docs/concepts/` or the most relevant existing section\n- **New internal architecture/design decision** → `docs/design/`\n- **New operational guide (config, deployment)** → `docs/operations/`\n- **New troubleshooting entry** → `docs/troubleshooting/`\n- **New reference material (glossary term, crate)** → `docs/reference/`\n- **Entirely new section** → create subdir with README.md hub, add to SUMMARY.md\n\n**Authoring conventions:**\n- One topic per file; use stable capability-based names (e.g., `sparql.md` not `sparql-v1.1.md`)\n- Use relative links within docs/ (e.g., `../concepts/time-travel.md`)\n- Structure: Title → Overview → Subtopics → Code examples → Related docs / See also\n- Include bash/JSON/SPARQL code blocks for examples\n- `docs/contributing/README.md` is the detailed contributor guide (372 lines) covering style, PR format, and workflow — it is NOT just a hub page like other section READMEs." ;
    mem:tag "README" ;
    mem:tag "SUMMARY.md" ;
    mem:tag "authoring" ;
    mem:tag "contributing" ;
    mem:tag "docs" ;
    mem:tag "documentation" ;
    mem:tag "project-structure" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:artifactRef "docs/README.md" ;
    mem:artifactRef "docs/SUMMARY.md" ;
    mem:artifactRef "docs/contributing/README.md" ;
    mem:branch "feature/config-graph" ;
    mem:createdAt "2026-02-26T23:02:20.397376+00:00"^^xsd:dateTime ;
    mem:factKind "architecture" .

mem:fact-01kjg3zmevnz6mdn53wkqn08p0 a mem:Fact ;
    mem:content "Fluree uses CIDv1 (Content Identifier v1) for all content-addressed blobs. Implementation is in fluree-db-core/src/content_id.rs wrapping the `cid` crate's `Cid` struct.\n\n**Format:** CIDv1 = version(1) + multicodec + multihash(SHA2-256, 32 bytes)\n\n**Multicodec values** (private-use range 0x300000+, defined in fluree-db-core/src/content_kind.rs):\n- 0x300001 Commit, 0x300002 Txn, 0x300003 IndexRoot, 0x300004 IndexBranch, 0x300005 IndexLeaf\n- 0x300006 DictBlob, 0x300007 Garbage, 0x300008 LedgerConfig, 0x300009 StatsSketch\n- 0x30000A GraphSourceSnapshot, 0x30000B SpatialIndex\n\n**Key APIs:**\n- `ContentId::new(kind, bytes)` — hashes bytes with SHA-256, tags with Fluree multicodec\n- `ContentId::verify(bytes)` — re-hashes and compares (NOT valid for commit-v2 blobs which exclude trailing hash+sig)\n- `ContentId::digest_hex()` — 64-char hex SHA-256 hash string\n- `ContentId::codec()` — returns the multicodec u64 value\n- Display uses base32-lower multibase encoding (produces `bafy...`/`bafk...` prefixes)\n- `from_codec(u64) -> Option<ContentKind>` reverse mapping\n\n**ContentKind enum** maps each blob type to its codec. `DictBlob { dict: DictKind }` sub-kinds all share one codec (0x300006); the dict sub-kind is in the stored bytes, not the CID.\n\n**Crate dependencies:** `cid = \"0.11\"`, `multihash = \"0.19\"`, `sha2 = \"0.10\"`" ;
    mem:tag "CIDv1" ;
    mem:tag "SHA-256" ;
    mem:tag "architecture" ;
    mem:tag "content-id" ;
    mem:tag "content-kind" ;
    mem:tag "multicodec" ;
    mem:tag "multihash" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:artifactRef "fluree-db-core/src/content_id.rs" ;
    mem:artifactRef "fluree-db-core/src/content_kind.rs" ;
    mem:branch "feature/ipfs" ;
    mem:createdAt "2026-02-27T17:57:43.003928+00:00"^^xsd:dateTime ;
    mem:factKind "architecture" .

mem:fact-01kjj80gw99c7hp8qh0tfp60kg a mem:Fact ;
    mem:content "IPFS storage backend (fluree-db-storage-ipfs crate) — full implementation details:\n\n**Crate structure:**\n- `src/lib.rs` — IpfsStorage struct implementing ContentStore + full Storage trait (StorageRead, StorageWrite, ContentAddressedWrite, StorageMethod)\n- `src/kubo.rs` — Thin HTTP RPC client for Kubo (block/put, block/get, block/stat, pin/add, pin/rm, is_pinned, id). Uses `reqwest` with multipart form upload.\n- `src/address.rs` — Parses `fluree:ipfs://ledger/kind/{hash}.ext` addresses. `extract_hash_hex()` gets the 64-char SHA-256 hex from the filename stem. `hash_hex_to_cid_string()` constructs a raw-codec CIDv1 for retrieval.\n- `src/error.rs` — IpfsStorageError enum (ConnectionFailed, NotFound, Rpc, Http, CidMismatch, Other). Implements `From<IpfsStorageError> for fluree_db_core::error::Error`.\n- `tests/it_kubo_block_api.rs` — 8 integration tests gated behind `integration-tests` feature. Require running Kubo at localhost:5001.\n\n**Kubo codec/pin behavior (verified by integration tests):**\n- `block/put` with Fluree's private-use multicodec (e.g. `cid-codec=0x300001`) — Kubo ACCEPTS it, stores the block, returns a CID with that codec\n- `block/get` with cross-codec CID — Kubo resolves by multihash regardless of codec, so a raw-codec CID retrieves blocks stored with any Fluree codec\n- `pin/add` with Fluree-codec CID — Kubo REJECTS it (\"no decoder registered for multicodec code 0x300001\") because pin/add tries to decode the block for DAG traversal\n- **Workaround:** pin using a raw-codec (0x55) CID built from the same SHA-256 multihash. `IpfsStorage::to_raw_cid()` and `address::hash_hex_to_cid_string()` handle this. Same approach for pin/rm (unpin).\n\n**Key design decisions:**\n- `put()` stores with Fluree's native multicodec — preserves CID identity in IPFS\n- Retrieval uses raw-codec CID constructed from address hash hex\n- `delete()` is a no-op (IPFS is immutable; GC via unpin)\n- `list_prefix()` returns error — NOT needed because: (1) nameservice always runs locally (file/DynamoDB) and handles all_records/all_graph_source_records, (2) drop_ledger Hard mode should walk commit chain + index chain + tree structure to discover CIDs rather than listing by prefix (existing code: walk_commit_chain_full, walk_prev_index_chain, clean_garbage, load_from_root_v5)\n\n**Feature flag chain:** cli/ipfs → server/ipfs → api/ipfs → connection/ipfs → dep:fluree-db-storage-ipfs\n\n**Connection config:**\n- JSON-LD: `ipfsApiUrl` and `ipfsPinOnPut` fields on Storage node (vocab constants in fluree-db-connection/src/vocab.rs)\n- Flat JSON: `@type: \"IpfsStorage\"` with `ipfsApiUrl`/`ipfsPinOnPut` fields\n- IpfsStorageConfig struct in fluree-db-connection/src/config.rs\n- IpfsConnection type alias and connect_ipfs factory in fluree-db-connection/src/ipfs.rs\n\n**Documentation:** docs/operations/ipfs-storage.md (comprehensive guide), updates to storage.md, connection-config-jsonld.md, SUMMARY.md" ;
    mem:tag "CID" ;
    mem:tag "IPFS" ;
    mem:tag "architecture" ;
    mem:tag "codec" ;
    mem:tag "connection" ;
    mem:tag "feature-flags" ;
    mem:tag "integration-tests" ;
    mem:tag "kubo" ;
    mem:tag "pinning" ;
    mem:tag "storage" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:artifactRef "docs/operations/ipfs-storage.md" ;
    mem:artifactRef "fluree-db-connection/src/config.rs" ;
    mem:artifactRef "fluree-db-connection/src/ipfs.rs" ;
    mem:artifactRef "fluree-db-storage-ipfs/src/address.rs" ;
    mem:artifactRef "fluree-db-storage-ipfs/src/error.rs" ;
    mem:artifactRef "fluree-db-storage-ipfs/src/kubo.rs" ;
    mem:artifactRef "fluree-db-storage-ipfs/src/lib.rs" ;
    mem:artifactRef "fluree-db-storage-ipfs/tests/it_kubo_block_api.rs" ;
    mem:branch "feature/ipfs" ;
    mem:supersedes mem:fact-01kjg407n8cqjy24njyyhz6dnz ;
    mem:createdAt "2026-02-28T13:46:35.273059+00:00"^^xsd:dateTime ;
    mem:factKind "architecture" .

mem:fact-01kjjd0b982vvxq7kxvn9bs90s a mem:Fact ;
    mem:content "Historical time-travel views must attach a BinaryRangeProvider when loading a BinaryIndexStore. `LedgerSnapshot::from_root_bytes()` produces a metadata-only snapshot with `range_provider=None`, so `Fluree::load_graph_db_at_t()` needs to set `LedgerSnapshot.range_provider = Some(BinaryRangeProvider::new(store, dict_novelty))` after loading the binary store; otherwise range-based operators can error with: \"binary-only db has no range_provider attached — load and attach BinaryIndexStore before queries\"." ;
    mem:tag "binary-index" ;
    mem:tag "binaryrangeprovider" ;
    mem:tag "bugfix" ;
    mem:tag "historical-view" ;
    mem:tag "range-provider" ;
    mem:tag "time-travel" ;
    mem:scope mem:repo ;
    mem:sensitivity "internal" ;
    mem:artifactRef "fluree-db-api/src/view/fluree_ext.rs" ;
    mem:artifactRef "fluree-db-core/src/db.rs" ;
    mem:artifactRef "fluree-db-core/src/range.rs" ;
    mem:createdAt "2026-02-28T15:13:52.424054+00:00"^^xsd:dateTime ;
    mem:factKind "architecture" .

mem:fact-01kjjmk1nva80w5cj3ey87asnj a mem:Fact ;
    mem:content "Global LRU \"leaflet cache\" (single memory budget):\n- Implementation: `fluree-db-binary-index/src/read/leaflet_cache.rs` (`LeafletCache`) uses a single `moka::sync::Cache` (TinyLFU) shared across decoded entry types (R1/R2 leaflet regions + dict leaves + BM25 leaflets + vector shards). One pool = one cache budget.\n- Budget control: `FLUREE_LEAFLET_CACHE_BYTES` (default 8 GiB) sets the max bytes for the cache.\n- Wiring: enabled by default in `fluree-db-api/src/ledger_manager.rs` (`LedgerManagerConfig::default()` creates `LeafletCache::with_max_bytes(...)`), and used by `BinaryIndexStore` load paths when ledger caching is on.\n- Note: Region 3 (history journal) is intentionally never cached (cold path)." ;
    mem:tag "binary-index" ;
    mem:tag "cache" ;
    mem:tag "leaflet-cache" ;
    mem:tag "lru" ;
    mem:tag "moka" ;
    mem:tag "performance" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:artifactRef "fluree-db-api/src/ledger_manager.rs" ;
    mem:artifactRef "fluree-db-binary-index/src/read/leaflet_cache.rs" ;
    mem:createdAt "2026-02-28T17:26:25.211748+00:00"^^xsd:dateTime ;
    mem:factKind "architecture" .

mem:fact-01kjjn8kspr3gnkcsmq9t1fsvf a mem:Fact ;
    mem:content "API-level ledger-info caching (for upstream apps):\n- Location: `fluree-db-api/src/ledger_info.rs` (`LedgerInfoBuilder::execute`).\n- Cache backend: the global unified `LeafletCache` (TinyLFU) so ledger-info shares the single memory budget with other cached decode artifacts.\n- Budget control: `FLUREE_LEAFLET_CACHE_BYTES` (default 8 GiB).\n- Key: xxh3_128 hash of a canonical string including (ledger_id, commit_t=ledger.t(), index_t=ledger.snapshot.t, realtime_property_details, include_property_datatypes, graph selector, context JSON hash).\n- Value: serialized JSON bytes (`Arc<[u8]>`) stored in `fluree-db-binary-index/src/read/leaflet_cache.rs` under a new `LedgerInfo` entry type.\n- Behavior: cache hit returns parsed JSON without recomputing novelty-merged stats; cache naturally busts when commit_t or index_t changes." ;
    mem:tag "api" ;
    mem:tag "cache" ;
    mem:tag "leaflet-cache" ;
    mem:tag "ledger-info" ;
    mem:tag "moka" ;
    mem:tag "performance" ;
    mem:tag "tinylfu" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:artifactRef "fluree-db-api/src/ledger_info.rs" ;
    mem:artifactRef "fluree-db-api/src/ledger_manager.rs" ;
    mem:artifactRef "fluree-db-binary-index/src/read/leaflet_cache.rs" ;
    mem:createdAt "2026-02-28T17:38:11.894884+00:00"^^xsd:dateTime ;
    mem:factKind "architecture" .

mem:fact-01kjjswkfg62ratezvpdchg30h a mem:Fact ;
    mem:content "Leaflet region decode behavior (read-side): `BinaryCursor` decodes Region 1 first and only decodes Region 2 (dt/t/lang/i) when (a) the caller requested metadata via `need_region2` AND there is at least one Region-1 match in that leaflet, OR (b) overlay merge is active, OR (c) time-travel replay is active. Region 1 and Region 2 are cached independently in the unified `LeafletCache` (TinyLFU) so caching R1 does not imply caching R2. Region 3 (history journal) is decoded only for time-travel and is intentionally never cached; however leaf files are currently fetched/mmap'd as whole objects, so Region 3 bytes still contribute to leaf download size for non-history queries." ;
    mem:tag "binary-index" ;
    mem:tag "cache" ;
    mem:tag "leaflet" ;
    mem:tag "overlay" ;
    mem:tag "performance" ;
    mem:tag "region1" ;
    mem:tag "region2" ;
    mem:tag "region3" ;
    mem:tag "time-travel" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:artifactRef "fluree-db-binary-index/src/format/leaflet.rs" ;
    mem:artifactRef "fluree-db-binary-index/src/read/binary_cursor.rs" ;
    mem:artifactRef "fluree-db-binary-index/src/read/leaflet_cache.rs" ;
    mem:createdAt "2026-02-28T18:59:01.232091+00:00"^^xsd:dateTime .

mem:fact-01kjp8g2s9pfh64sbx1crx42d8 a mem:Fact ;
    mem:content "Query API layering in fluree-db-api:\n- `Fluree::query_connection(...)` is the request-boundary entrypoint. It parses dataset spec/options from the query itself, chooses the execution model, and applies per-source/global policy and config-graph defaults. It uses GraphDb for single-ledger (including time travel via spec) and DataSetDb for multi-ledger union.\n- `Fluree::query(&GraphDb, ...)` is the stable single-ledger \"db is a value\" API. You load a `GraphDb` once via `db() / db_at_t() / db_at()` (supports `#txn-meta` / named graph selection) and then run many queries with uniform wrapper behavior (policy/reasoning/config).\n- `DataSetDb` supports a Fluree history/changes mode via `history_range: Option<(from_t,to_t)>`; dataset execution threads `history_mode`, `from_t`, and `to_t` into the query engine (`ContextConfig`) enabling time-range / @op semantics." ;
    mem:tag "api-layering" ;
    mem:tag "dataset" ;
    mem:tag "fluree-db-api" ;
    mem:tag "graphdb" ;
    mem:tag "history" ;
    mem:tag "query" ;
    mem:tag "query-connection" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:artifactRef "fluree-db-api/src/query/connection.rs" ;
    mem:artifactRef "fluree-db-api/src/view/dataset.rs" ;
    mem:artifactRef "fluree-db-api/src/view/dataset_query.rs" ;
    mem:artifactRef "fluree-db-api/src/view/fluree_ext.rs" ;
    mem:artifactRef "fluree-db-api/src/view/query.rs" ;
    mem:supersedes mem:fact-01kjn63vy23w6kehy3gbs30ncv ;
    mem:createdAt "2026-03-02T03:12:02.857222+00:00"^^xsd:dateTime ;
    mem:factKind "api" .

mem:fact-01kjt5bpzz5aqvv93syafs18m1 a mem:Fact ;
    mem:content "Fast GROUP BY ?o COUNT(?s) top-k optimization is now generic for any bound predicate in POST: `PredicateGroupCountFirstsOperator` scans leaflets using uncompressed per-leaflet FIRST (p,o) metadata and uses boundary-equality (FIRST(i)==FIRST(i+1)) to add `row_count` without decoding. It consults `StatsView::get_graph_property(g_id,p_id)` at open-time: if exactly one datatype and it maps to a reserved dt_id (and is not langString), it avoids decoding Region2 entirely and emits `EncodedLit` with constant dt_id/lang_id=0; otherwise it falls back to decoding Region2 and groups by (o_kind,o_key,dt_id,lang_id) for correctness." ;
    mem:tag "count" ;
    mem:tag "datatype" ;
    mem:tag "group-by" ;
    mem:tag "leaflet" ;
    mem:tag "optimization" ;
    mem:tag "post-index" ;
    mem:tag "region2" ;
    mem:tag "sparql" ;
    mem:tag "statsview" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:artifactRef "fluree-db-query/src/execute/operator_tree.rs" ;
    mem:artifactRef "fluree-db-query/src/fast_group_count_firsts.rs" ;
    mem:createdAt "2026-03-03T15:34:11.711499+00:00"^^xsd:dateTime .

mem:fact-01kjtbbfnpgec8tk0zqjeg7sb8 a mem:Fact ;
    mem:content "Added POST leaflet-skipping fast path for bound-object COUNT queries: `PredicateObjectCountFirstsOperator` answers `SELECT (COUNT(?s) AS ?c) WHERE { ?s <p> <o> }` (single triple, bound predicate+object, no dt/lang constraint, no GROUP BY) by scanning POST leaflets using uncompressed FIRST (p,o) metadata and boundary-equality to add `row_count` without decoding; falls back to decoding Region1 for partial leaflets. Wired via `detect_predicate_object_count` in `execute/operator_tree.rs`, and preserves existing loose semantics when `tp.dtc` is None (ignores dt/lang)." ;
    mem:tag "count" ;
    mem:tag "fast-path" ;
    mem:tag "leaflet" ;
    mem:tag "optimization" ;
    mem:tag "post-index" ;
    mem:tag "sparql" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:artifactRef "fluree-db-query/src/execute/operator_tree.rs" ;
    mem:artifactRef "fluree-db-query/src/fast_group_count_firsts.rs" ;
    mem:createdAt "2026-03-03T17:18:55.670244+00:00"^^xsd:dateTime .

mem:fact-01kjte0r70p5ppv4cjkfyyedpe a mem:Fact ;
    mem:content "SPARQL Testsuite: Critical Gotchas & How to Run\n\n- **MUST `cd testsuite-sparql/` first** — it's in workspace `exclude = []` (root Cargo.toml line 41-42), so `cargo test -p testsuite-sparql` from workspace root FAILS with \"did not match any packages\"\n- Syntax tests run by default (`cargo test`); eval tests are `#[ignore]` and need `--include-ignored`\n- Each test runs in a subprocess (`run-w3c-test` binary) with 5s (syntax) / 10s (eval) timeouts — panics/hangs show as \"Subprocess error\"\n- W3C test data lives in git submodule `testsuite-sparql/rdf-tests/` (from github.com/w3c/rdf-tests)\n- W3C URL→local path: strip `https://w3c.github.io/rdf-tests/`, prepend `testsuite-sparql/rdf-tests/`\n- Tests declared in `tests/w3c_sparql.rs` — each function calls `check_testsuite(manifest_url, ignored_tests)`\n- Result comparison is isomorphic (order-independent, blank-node-relabeling-tolerant)\n- Key commands (from testsuite-sparql/):\n  - `make test-eval-cat CAT=functions` — single eval category\n  - `make count-eval` — quick pass/fail summary\n  - `make report-eval-json` — JSON report (best for analysis) → report-eval.json\n  - `make show-query TEST=strdt01.rq` — read test query file\n  - `make report-10-json` — SPARQL 1.0 JSON report → report-10.json\n- `make investigate` only searches syntax `report.txt`, NOT eval `report-eval.txt` — use `make investigate-eval` or grep manually for eval failures\n- JSON report format: `{ total, passed, failed, pass_rate, tests: [{ test_id, status, error, timeout }] }`\n- SPARQL 1.1 eval categories: aggregates, bind, bindings, cast, construct, exists, functions, grouping, negation, project_expression, property_path, subquery" ;
    mem:tag "commands" ;
    mem:tag "compliance" ;
    mem:tag "gotchas" ;
    mem:tag "testing" ;
    mem:tag "testsuite-sparql" ;
    mem:tag "w3c" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:artifactRef "Cargo.toml" ;
    mem:artifactRef "testsuite-sparql/Cargo.toml" ;
    mem:artifactRef "testsuite-sparql/Makefile" ;
    mem:artifactRef "testsuite-sparql/tests/w3c_sparql.rs" ;
    mem:branch "main" ;
    mem:createdAt "2026-03-03T18:05:29.696388+00:00"^^xsd:dateTime ;
    mem:factKind "command" .

mem:fact-01kjte15ttyc87azr53xda0bzg a mem:Fact ;
    mem:content "Testsuite JSON Report Analysis Recipes (dry-run validated 2026-03-03)\n\nNo built-in Makefile targets for per-category breakdowns or error classification. Use `make summary` and `make classify` (added as new targets), or these Python snippets for ad-hoc analysis (all assume `cd testsuite-sparql/`):\n\n**Generate reports first:**\n- `make report-eval-json` → report-eval.json (SPARQL 1.1, ~5 min)\n- `make report-10-json` → report-10.json (SPARQL 1.0, ~5 min)\n\n**Per-category breakdown** (multi-line version):\n```python\nimport json; from collections import defaultdict\nr = json.load(open('report-eval.json'))\ncats = defaultdict(lambda: {'pass':0,'fail':0,'total':0})\nfor t in r['tests']:\n    parts = t['test_id'].split('/')\n    cat = next((parts[i+1] for i,p in enumerate(parts) if p=='data-sparql11' and i+1&lt;len(parts)),'unknown')\n    cats[cat]['total'] += 1\n    cats[cat]['pass' if t['status']=='pass' else 'fail'] += 1\nfor cat in sorted(cats):\n    c=cats[cat]; rate=f\"{100*c['pass']/c['total']:.0f}%\"\n    print(f\"{cat:&lt;22} {c['pass']:>4}/{c['total']:&lt;4} {rate}\")\n```\n(For SPARQL 1.0: change `data-sparql11` to `data-r2` and read `report-10.json`)\n\n**Error classification** (group failures by root cause):\n- `t.get('timeout')` → TIMEOUT\n- `'Subprocess error' in err and 'panicked' in err` → PANIC (check JSON report for full stack trace)\n- `'SPARQL error' in err or 'unexpected character' in err` → PARSE/LOWERING ERROR\n- `'Internal' in err` → INTERNAL ERROR (execution bug)\n- `'not isomorphic' in err and 'got 0' in err` → EMPTY RESULTS (data load or query match issue)\n- `'not isomorphic' in err` → RESULT MISMATCH (wrong values)\n- `'not yet implemented' in err` → NOT IMPLEMENTED\n\n**List failures for a specific category:**\n```python\nimport json\nr = json.load(open('report-eval.json'))\nfor t in r['tests']:\n    if t['status']=='fail' and '/functions/' in t['test_id']:\n        tid = t['test_id'].split('#')[-1]\n        err = t['error'].strip().split('\\n')[0][:80]\n        print(f\"{tid}: {err}\")\n```" ;
    mem:tag "analysis" ;
    mem:tag "json-report" ;
    mem:tag "python" ;
    mem:tag "reporting" ;
    mem:tag "testsuite-sparql" ;
    mem:tag "workflow" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:artifactRef "testsuite-sparql/Makefile" ;
    mem:artifactRef "testsuite-sparql/src/report.rs" ;
    mem:branch "main" ;
    mem:createdAt "2026-03-03T18:05:43.642584+00:00"^^xsd:dateTime ;
    mem:factKind "command" .

mem:fact-01kjte1yw4jyrqvnpq27mr2m9v a mem:Fact ;
    mem:content "SPARQL ↔ JSON-LD Query Shared Execution Architecture\n\nBoth SPARQL and JSON-LD queries compile to the same IR and execute on the same engine:\n\nCompilation paths:\n- SPARQL: `fluree-db-sparql/src/lower/` → `lower_sparql()` → IR (`fluree-db-query/src/ir.rs`)\n- JSON-LD: `fluree-db-query/src/parse/` → `parse_query_ast()` + `lower_query()` → same IR\n\nConvergence point:\n- `fluree-db-api/src/view/query.rs::query()` → `build_executable_for_view()` → `execute_view_internal()`\n- Both languages produce identical `ParsedQuery` structures for equivalent queries\n\nShared constructs:\n- `Expression` enum (ir.rs): 66+ built-in functions — both languages use same eval in `fluree-db-query/src/expression/`\n- `AggregateFn` enum (aggregate.rs): COUNT, SUM, AVG, MIN, MAX, MEDIAN, VARIANCE, STDDEV, GROUP_CONCAT, SAMPLE\n- `Pattern` enum (ir.rs): Triple, Optional, Minus, Union, Filter, Bind, Values, PropertyPath, Subquery, IndexSearch, VectorSearch, GeoSearch, S2Search, Service, Graph, Exists, NotExists\n- `IriEncoder` trait: abstracts SID assignment so both frontends share the execution engine\n\nSPARQL-only paths (not shared with JSON-LD):\n- Property path lowering (`fluree-db-sparql/src/lower/path.rs`)\n- RDF-star quoted triples (`rdf_star.rs`)\n- ASK query form (`ask.rs`)\n- SPARQL Update (INSERT/DELETE)\n- Dataset clause validation" ;
    mem:tag "architecture" ;
    mem:tag "ir" ;
    mem:tag "jsonld" ;
    mem:tag "parity" ;
    mem:tag "query" ;
    mem:tag "shared-execution" ;
    mem:tag "sparql" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:artifactRef "fluree-db-api/src/view/query.rs" ;
    mem:artifactRef "fluree-db-query/src/aggregate.rs" ;
    mem:artifactRef "fluree-db-query/src/expression/" ;
    mem:artifactRef "fluree-db-query/src/ir.rs" ;
    mem:artifactRef "fluree-db-query/src/parse/lower.rs" ;
    mem:artifactRef "fluree-db-sparql/src/lower/" ;
    mem:branch "main" ;
    mem:createdAt "2026-03-03T18:06:09.284115+00:00"^^xsd:dateTime ;
    mem:factKind "architecture" .

mem:fact-01kjte2qeekkgz4dryv84sqq8e a mem:Fact ;
    mem:content "SPARQL Compliance Workflow: Failure → Fix → Validate (dry-run validated 2026-03-03)\n\nStep 1 — Assess current state (must cd first!):\n  cd testsuite-sparql\n  make count-eval                     # quick summary\n  make test-eval-cat CAT=functions    # focus on one category\n  make report-eval-json               # full JSON for analysis\n  make summary                        # per-category breakdown table\n  make classify                       # group failures by error type\n\nStep 2 — Classify failures from JSON report:\n  Use `make classify` or Python error classification to group into: PARSE/LOWERING, INTERNAL, RESULT MISMATCH, EMPTY RESULTS, NOT IMPLEMENTED, PANIC, TIMEOUT\n\nStep 3 — Read the W3C test files:\n  make show-query TEST=strdt01.rq                          # print the .rq query\n  cat rdf-tests/sparql/sparql11/functions/data.ttl          # shared test data\n  cat rdf-tests/sparql/sparql11/functions/strdt01.srx       # expected results\n  Note: some tests have per-test .ttl, others share data.ttl in the category dir.\n\nStep 4 — Root cause mapping:\n  PARSE/LOWERING → fluree-db-sparql/src/parser/ (parsing) or src/lower/ (IR lowering)\n  INTERNAL ERROR → fluree-db-query/src/execute.rs, src/expression/\n  RESULT MISMATCH → Execution produces wrong values — check eval logic\n  EMPTY RESULTS → Data not loaded or query returns no matches — check testsuite-sparql/src/query_handler.rs\n  PANIC → Unwrap/index bug — full stack trace in JSON report error field\n  NOT IMPLEMENTED → Handler returns early — search \"not yet implemented\" in testsuite-sparql/src/\n\nStep 5 — Fix and validate:\n  make test-eval-cat CAT=&lt;category&gt;     # category-level check\n  make count-eval                        # regression check\n  cd .. &amp;&amp; cargo clippy -p fluree-db-sparql -p fluree-db-query --all-features -- -D warnings\n  cargo test -p fluree-db-sparql &amp;&amp; cargo test -p fluree-db-query\n\nStep 6 — Check JSON-LD parity (per constraint):\n  If shared code changed: cargo test -p fluree-db-api --test it_query\n  If feature is expressible in JSON-LD: add a parallel JSON-LD test" ;
    mem:tag "compliance" ;
    mem:tag "debugging" ;
    mem:tag "dry-run" ;
    mem:tag "sparql" ;
    mem:tag "testsuite-sparql" ;
    mem:tag "workflow" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:artifactRef "docs/contributing/sparql-compliance.md" ;
    mem:artifactRef "testsuite-sparql/Makefile" ;
    mem:branch "main" ;
    mem:createdAt "2026-03-03T18:06:34.446036+00:00"^^xsd:dateTime ;
    mem:factKind "command" .

mem:fact-01kk1sq2xwmxsqe828pgk28svw a mem:Fact ;
    mem:content "Issue #55 W3C SPARQL Compliance Status (measured 2026-03-06 on fix/sparql-xsd-casts)\n\nMaster tracker: github.com/fluree/db-r/issues/55\n\nCurrent pass rate: 225/327 (68.8%) — up from 214/327 (65.4%) after #46 fix\n\nSPARQL 1.1 per-category:\n- negation: 12/12 (100%) — ✅ #54 closed\n- syntax-query: 80/94 (85%)\n- functions: 59/75 (79%) — ✅ #48 closed (PR #75)\n- exists: 4/6 (67%)\n- subquery: 9/14 (64%) — #52\n- project-expression: 4/7 (57%)\n- grouping: 3/6 (50%)\n- property-path: 16/33 (48%) — #47\n- aggregates: 22/46 (48%) — #61, #62\n- bindings: 3/11 (27%) — #51\n- bind: 2/10 (20%) — #51\n- cast: 6/6 (100%) — ✅ #46 (fix/sparql-xsd-casts branch)\n- construct: 0/7 (0%) — #53\n\nClosed since last update: #46 (PR pending)\nAlso closed: #48 (PR #75), #54 (PR #74)\n\nOpen: #47 (property paths), #50 (arithmetic), #51 (BIND/VALUES), #52 (subqueries), #53 (CONSTRUCT eval), #61 (aggregate expr input), #62 (numeric type promotion)" ;
    mem:tag "compliance" ;
    mem:tag "issue-55" ;
    mem:tag "sparql" ;
    mem:tag "status" ;
    mem:tag "test-counts" ;
    mem:tag "tracking" ;
    mem:tag "w3c" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:artifactRef "testsuite-sparql/tests/w3c_sparql.rs" ;
    mem:branch "main" ;
    mem:supersedes mem:fact-01kk1qbna7qqqk6hck165kcjcy ;
    mem:createdAt "2026-03-06T14:44:36.924914+00:00"^^xsd:dateTime ;
    mem:factKind "api" .

mem:fact-01kk20kbgep6w9wdw6tvs8tgsn a mem:Fact ;
    mem:content "Issue #55 W3C SPARQL Compliance Status (measured 2026-03-06)\n\nMaster tracker: github.com/fluree/db-r/issues/55\n\nCurrent pass rate: 225/327 (68.8%)\n\nSPARQL 1.1 per-category:\n- negation: 12/12 (100%)\n- cast: 6/6 (100%) — PR #83\n- syntax-query: 80/94 (85%)\n- functions: 59/75 (79%)\n- exists: 4/6 (67%)\n- subquery: 9/14 (64%) — #52\n- project-expression: 4/7 (57%)\n- grouping: 3/6 (50%)\n- property-path: 16/33 (48%) — #47\n- aggregates: 22/46 (48%) — #61, #62\n- bindings: 3/11 (27%) — #51\n- bind: 2/10 (20%) — #51\n- construct: 0/7 (0%) — #53\n\nOpen issues: #47 (property paths), #50 (arithmetic), #51 (BIND/VALUES), #52 (subqueries), #53 (CONSTRUCT eval), #61 (aggregate expr input), #62 (numeric type promotion)" ;
    mem:tag "compliance" ;
    mem:tag "issue-55" ;
    mem:tag "sparql" ;
    mem:tag "status" ;
    mem:tag "w3c" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:artifactRef "testsuite-sparql/tests/w3c_sparql.rs" ;
    mem:branch "main" ;
    mem:supersedes mem:fact-01kk1sq2xwmxsqe828pgk28svw ;
    mem:createdAt "2026-03-06T16:44:54.670405+00:00"^^xsd:dateTime ;
    mem:factKind "api" .

mem:fact-01kk9grz482xg3p6krjbpb1whk a mem:Fact ;
    mem:content "Issue #55 W3C SPARQL Compliance Status (measured 2026-03-09)\n\nMaster tracker: github.com/fluree/db-r/issues/55\n\nCurrent pass rate: 241/327 (73.7%)\n\nSPARQL 1.1 per-category:\n- bind: 10/10 (100%) — FIXED in #51\n- cast: 6/6 (100%) — PR #83\n- negation: 12/12 (100%)\n- bindings: 10/11 (91%) — FIXED in #51 (remaining: graph test needs named graph support)\n- syntax-query: 81/94 (86%)\n- functions: 59/75 (79%)\n- exists: 4/6 (67%)\n- subquery: 9/14 (64%) — #52\n- aggregates: 27/46 (59%)\n- project-expression: 4/7 (57%)\n- grouping: 3/6 (50%)\n- property-path: 16/33 (48%) — #47\n- construct: 0/7 (0%) — #53\n\nOpen issues: #47 (property paths), #50 (arithmetic), #52 (subqueries), #53 (CONSTRUCT eval), #61 (aggregate expr input), #62 (numeric type promotion)" ;
    mem:tag "compliance" ;
    mem:tag "issue-55" ;
    mem:tag "sparql" ;
    mem:tag "status" ;
    mem:tag "w3c" ;
    mem:scope mem:repo ;
    mem:sensitivity "public" ;
    mem:artifactRef "testsuite-sparql/tests/w3c_sparql.rs" ;
    mem:branch "main" ;
    mem:supersedes mem:fact-01kk20kbgep6w9wdw6tvs8tgsn ;
    mem:createdAt "2026-03-09T14:42:16.841273+00:00"^^xsd:dateTime ;
    mem:factKind "api" .
