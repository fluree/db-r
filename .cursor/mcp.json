{
  "mcpServers": {
    "fluree-memory": {
      "type": "stdio",
      "command": "/usr/local/bin/fluree",
      "args": [
        "mcp",
        "serve",
        "--transport",
        "stdio"
      ],
      "env": {
        "FLUREE_HOME": "${workspaceFolder}/.fluree"
      }
    }
  }
}